RewriteEngine On

# Route /verify and /deactivate to license-api.php with action param
RewriteRule ^verify$      license-api.php [L]
RewriteRule ^deactivate$  license-api.php [L]

# Block direct access to schema and any .sql files
<FilesMatch "\.sql$">
  Require all denied
</FilesMatch>
